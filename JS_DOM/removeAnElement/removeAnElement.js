//Saves the element that we want to remove.
let elementToRemove = document.getElementById('vespa');

//Gets the element with the ID of 'italy-attractions' from the document,
// then removes the child element designated in 'elementToRemove'.
document.getElementById('italy-attractions').removeChild(elementToRemove);