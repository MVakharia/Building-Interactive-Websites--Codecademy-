//This code won't execute in Node.js as it doesn't recognise the 'document' object:
// it will only execute in the web browser console.

//To view the output of the statement below, open selectModifyElements0.html in a web browser,
// then hit F12 and click on the 'Console' tab.

console.log(document.querySelector('p').innerHTML);

